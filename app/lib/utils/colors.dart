import 'package:app_9atra/utils/importations.dart';

class AppColors {
  // static const black_color_1 = Color(0xFF211E1F);
  // static const black_color_2 = Color(0xFF2B2024);

  // static const purple_color = Color(0xFF992149);
  // static const red_color = Color(0xFFE92727);
  // static const white_color = Color(0xFFFFFFFF);

  static const red_color = Color(
      0xFFD2374F); // You can use this color for light text button, icons, backgrounds, etc
  static const pink_color = Color(
      0xFFFAC4C8); // You can use this color for layers of shapes like waves and cercles, etc

  static const black_color = Color(
      0xFF363535); // You can use this color for any text except inactive text

  static const grey_color_1 = Color(
      0xFFE1E0E1); // You can use this color for background of inactive elements like buttons, input fields, icons etc
  static const grey_color_2 = Color(
      0xFF757575); // You can use this color for text of inactive elements like buttons, input fields, etc

  static const white_bg_color = Color.fromARGB(255, 247, 247,
      247); // You can use this color for background of light screens, light buttons, icons, etc
  static const white_color = Color(0xFFFFFFFF);

  static var transparent; // You can use this color for any text except inactive text or icons background
}
